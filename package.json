{
  "private": true,
  "name": "@fullcalendar/workspace",
  "version": "0.0.0",
  "type": "module",
  "devDependencies": {
    "@fullcalendar/workspace-scripts": "workspace:*",
    "turbo": "^1.4.6"
  },
  "scripts": {
    "pnpm:devPreinstall": "pnpm run -r --parallel preflight",
    "build": "turbo run preflight --filter './standard/**' --filter './premium/**' && turbo run tsc --filter './standard/**' --filter './premium/**' && turbo run build --filter './standard/**' --filter './premium/**'",
    "dev": "turbo run preflight --filter './standard/**' --filter './premium/**' && turbo run tsc --filter './standard/**' --filter './premium/**' && turbo run '//#tsc:dev' dev --parallel --no-cache --filter './standard/**' --filter './premium/**'",
    "clean": "turbo run clean",
    "tsc": "workspace-scripts-tsc -b",
    "tsc:dev": "workspace-scripts-tsc -b --watch --pretty --preserveWatchOutput"
  },
  "engines": {
    "node": ">=16",
    "pnpm": ">=7.8.0"
  },
  "pnpm": {
    "packageExtensions": {
      "@nuxt/components": {
        "peerDependenciesMeta": {
          "consola": {
            "optional": true
          }
        }
      }
    },
    "peerDependencyRules": {
      "allowAny": [
        "postcss"
      ]
    },
    "patchedDependencies": {
      "@pnpm/make-dedicated-lockfile@0.3.20": "scripts/patches/@pnpm__make-dedicated-lockfile@0.3.20.patch",
      "concurrently@7.3.0": "scripts/patches/concurrently@7.3.0.patch"
    }
  }
}
