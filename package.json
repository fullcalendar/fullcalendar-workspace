{
  "private": true,
  "name": "@fullcalendar/workspace",
  "version": "0.0.0",
  "type": "module",
  "devDependencies": {
    "@fullcalendar/workspace-scripts": "workspace:*"
  },
  "scripts": {
    "postinstall": "workspace-scripts postinstall",
    "lint": "workspace-scripts lint",
    "build": "workspace-scripts build",
    "dev": "workspace-scripts dev",
    "test": "workspace-scripts test",
    "clean": "workspace-scripts clean"
  },
  "monorepoConfig": {
    "archiveSubtrees": [
      "./standard",
      "./premium"
    ],
    "filterSubtrees": [
      "./standard",
      "./premium",
      "./scripts"
    ]
  },
  "engines": {
    "pnpm": ">=7.9.5"
  },
  "pnpm": {
    "packageExtensions": {
      "@nuxt/components": {
        "peerDependenciesMeta": {
          "consola": {
            "optional": true
          }
        }
      }
    },
    "peerDependencyRules": {
      "allowAny": [
        "postcss"
      ]
    },
    "patchedDependencies": {
      "@pnpm/make-dedicated-lockfile@0.3.20": "scripts/patches/@pnpm__make-dedicated-lockfile@0.3.20.patch"
    }
  }
}
