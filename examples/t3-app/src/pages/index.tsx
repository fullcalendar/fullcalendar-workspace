import { type NextPage } from "next";
import Layout from '../components/layout';

const Home: NextPage = () => {
  return (
    <Layout>
      Welcome! Click the links above
    </Layout>
  );
};

export default Home;
